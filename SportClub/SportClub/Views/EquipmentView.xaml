<UserControl x:Class="SportClub.Views.EquipmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Зал" Binding="{Binding Room.Name}" Width="auto"/>
                <DataGridTextColumn Header="Состояние" Binding="{Binding Condition.Name}" Width="auto"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,5">
            <Button Content="Загрузить"   Command="{Binding LoadCommand}"   Margin="2"/>
            <Button Content="Добавить"    Command="{Binding AddCommand}"    Margin="2"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}" Margin="2"/>
        </StackPanel>
    </Grid>
</UserControl>
