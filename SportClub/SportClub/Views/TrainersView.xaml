<UserControl x:Class="SportClub.Views.TrainersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNum}" Width="120"/>
                <DataGridTextColumn Header="Специализация" Binding="{Binding Specialization.Name}" Width="150"/>
                <DataGridTextColumn Header="Дата принятия" Binding="{Binding DateOfAcceptance}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,5">
            <Button Content="Load"   Command="{Binding LoadCommand}"   Margin="2"/>
            <Button Content="Add"    Command="{Binding AddCommand}"    Margin="2"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="2"/>
        </StackPanel>
    </Grid>
</UserControl>
